export const ACCOUNT = 'Accounts';
export const LOCATION = 'Locations';
export const PRODUCT = 'Products';

export const MAIN_PATH = "Main";

export const navigationLinks = [ {path: "/" + MAIN_PATH.toLowerCase(), label: MAIN_PATH},{path: "/" + ACCOUNT.toLowerCase(), label: ACCOUNT},
    {path: "/" + LOCATION.toLowerCase(), label: LOCATION},
    {path: "/" + PRODUCT.toLowerCase(), label: PRODUCT} ]

    